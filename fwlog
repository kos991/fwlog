#!/bin/bash
# 快捷键包装脚本 - 直接调出菜单

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MAIN_SCRIPT="$SCRIPT_DIR/sangforfw_log.sh"

# 如果没有参数，直接启动交互式菜单
if [ $# -eq 0 ]; then
    exec "$MAIN_SCRIPT"
else
    # 有参数时传递给主脚本
    exec "$MAIN_SCRIPT" "$@"
fi
