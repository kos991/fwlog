# v1.0.4 紧急修复

## 修复内容

- 修复数据概览顶部 CPU、内存、数据库状态在窄宽度下被截断的问题，改为透明小折线状态展示。
- RPM/DEB 升级前自动备份 `app_settings`，升级后在 ClickHouse 就绪后恢复，避免扫描计划、日志源、IP 库路径、升级开关和管理员密码哈希等运行配置被默认值覆盖。

## 验收重点

- 数据概览顶部 CPU、内存、数据库状态完整可读，样式保持透明轻量。
- 从旧版本升级后，系统设置页的扫描计划、日志源、IP 库路径和登录密码不回到默认值。
