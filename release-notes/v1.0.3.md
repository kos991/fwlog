# v1.0.3

## 修复

- 修复修改管理员密码后只在当前进程内生效的问题，升级或重启后不再回到默认 `admin`。
- 启动时会加载已保存的管理员密码哈希，异常格式会自动忽略，避免坏配置导致无法登录。
- 新密码限制为 6-18 个字符，仅允许 `a-z`、`A-Z`、`0-9` 和 `-_.` 字符。
- 数据概览页“日志趋势”接入最近 24 小时真实日志量，不再使用前端写死的示例数据。

## 改进

- 数据概览页顶部 CPU、内存、数据库状态改为透明状态条，降低视觉干扰。
