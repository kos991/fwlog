## 更新说明

- 监控大屏新增“端口分布”，按目标端口统计 Top 10，并对常见服务端口显示易读标签，例如 `80:http`、`443:https`、`53:dns`、`22:ssh`。
- 修复开启“自动增量同步”后，在空日志目录或空索引场景下反复进入全量重建的问题。现在当日志目录没有可同步文件且索引表已存在时，增量同步会直接完成，不再循环重建。

## 验证

- GitHub Actions `CI` 已通过 `Test Linux amd64`。
- GitHub Actions `CI` 已通过 `Test Kylin V10 SP1 x86_64 baseline`。

## 部署提示

银河麒麟 V10 SP1 x86_64 服务器请使用 `nat-query-service_kylin-v10-sp1_x86_64.tar.gz`。
