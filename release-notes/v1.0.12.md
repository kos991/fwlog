# v1.0.12

## 本次修复

- 修复在线升级安装 `fwlog-upgrade` 时在受限服务环境下失败的问题。
- 升级安装过程改为通过独立的 `systemd-run` 一次性任务执行，避免被当前服务的只读系统沙箱拦截。
- 升级临时包下载目录改到 `/opt/nat-query/tmp`，避免 `PrivateTmp` 导致的临时文件不可见问题。

## 验证范围

- 升级安装命令测试
- 在线升级执行链路测试
- `go test ./internal/app -run Upgrade`
- `go test ./...`
